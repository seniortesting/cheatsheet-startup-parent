const fs = require('fs')
module.exports = function () {
    // fs.writeFileSync('teardown.log', 'javascript is the best language')
    // console.log(fs.readFileSync('teardown.log', {encoding: 'utf-8'}))
}
